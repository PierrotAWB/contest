#include <algorithm>
#include <bitset>
#include <fstream>
#include <iostream>
#include <map>
#include <math.h>
#include <set>
#include <string>
#include <vector>

#define watch(x) cout << "CURRENT VALUE: " << x << endl;

using namespace std; 

int N;

int main() {
	ios::sync_with_stdio(0);
	cin.tie();
	
	cin >> N; 
	cout << N << "\n";

	for (int i=0; i < N; i++) {
		cout << i << " " << "\n";
	}
	
	return 0;
}